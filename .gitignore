# data & cache — never commit scraped pages or datasets
data/
*.parquet
*.csv

# ...except the sanitized, title-stripped public dataset the deployed
# dashboard reads (built by src/make_public_dataset.py — aggregate-only, no PII)
!data_public/
!data_public/listings_public.parquet

# secrets & local config
.env
config.json

# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
