# Runtime deps — what the deployed Streamlit dashboard needs.
# (Collection / analysis / test tooling lives in requirements-dev.txt.)
streamlit>=1.34
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
scikit-learn>=1.5
